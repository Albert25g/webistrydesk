#!/usr/bin/env bash

# Husky v10 compatibility: avoid sourcing _/husky.sh in the hook.
# Keep the same checks below — uncomment as needed.
# Run environment check and lint before push
# bash scripts/check-env-not-tracked.sh
# npm run lint

exit 0
